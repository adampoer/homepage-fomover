function uploadImage(image) {
    // save the uploaded image to a temporary folder
    // update the clothing mockup image to the uploaded image
    clothingMockup.setImage(image);
    clothingMockup.PickSize(image)
  }
  


